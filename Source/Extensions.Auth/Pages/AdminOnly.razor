@rendermode InteractiveServer
@inherits PageBase

<PageTitle>@T("Admin only")</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">@T("Admin only")</MudText>

<MudAlert Severity="Severity.Success" Variant="Variant.Outlined" Class="mb-4">
    @T("You see this because you hold settings.write (or a wildcard like '*').")
</MudAlert>

<MudButton Variant="Variant.Outlined" Href="@RequirePermissionDemo.Route"
           StartIcon="@Icons.Material.Filled.ArrowBack">
    @T("Back")
</MudButton>
